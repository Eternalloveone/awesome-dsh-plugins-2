# 2026-08-17

## dsh-plugin topic sweep & catalog additions
- Scanned GitHub topic `dsh-plugin` via WebFetch pagination (pages 1–16, ~320 repos; API rate-limited/unreachable, no token).
- Diffed every topic repo against `repositories.csv` + `dsh-plugin-topic-candidates.csv`.
- Catalog was already very current (last snapshot 2026-08-17 13:43): only **5 genuinely new repos (all ≥10★)** found.
- Launched parallel Agent subagents (4 + 1) to investigate each new repo and produce structured, criteria-compliant catalog entries.
- Merge results (backups in /tmp/dsh_backup_20260817_160036_*):
  - `repositories.csv` +2 (main dir, genuine DSH load path):
    - CSlawyer1985/claude-for-legal-ZH (725★) — dsh_skill, legal, Apache-2.0
    - syncable-dev/memtrace-public (452★) — installable_dsh_plugin, memory, verified (DSH bundle dsh-plugin-memtrace)
  - `dsh-plugin-topic-candidates.csv` +5: the two above + Anil-matcha/awesome-deepseek-harness (917★, related/awesome-list), salathleizhang/deepseek-harness-desktop (121★, watchlist/desktop-shell), geml-spec/geml (24★, related — actually Claude Code plugin, not DSH)
  - `verified-plugins.csv` +2: CSlawyer1985, syncable-dev
- Note: Anil-matcha/awesome-deepseek-harness, salathleizhang desktop shell, and geml-spec/geml have NO DSH load path → kept out of main dir per curation-criteria.yaml (aggregate lists / desktop shells / non-DSH excluded).
- Reusable workflow: topic-scan → diff vs catalog CSVs → parallel agents → merge. Candidate for a saved skill.

## Continuation sweep (pages 17–24) + skill creation
- Continued WebFetch pagination to page 24. Pages 17–20: 1 new repo above floor →
  `G36maid/deepseek-harness` (13★). Agent investigation: it is a **zh-TW localization fork of
  the DSH core harness itself** (not a third-party extension) → verdict `related`, candidate
  list only (NOT main dir, NOT verified-plugins).
- Pages 21–24 all ≤10★. GitHub topic pages sort by stars descending, so all deeper pages are
  also ≤10★ → sweep complete. Floor rule: skip stars ≤10 ("10 个 star 内的先不处理").
- Merge: `dsh-plugin-topic-candidates.csv` 1996 → **1997** (added G36maid as related).
  Backups /tmp/dsh_backup_20260817_161025_*.
- **Created project skill `dsh-topic-curator`** at
  `.workbuddy/skills/dsh-topic-curator/` with SKILL.md + references/{curation-criteria,csv-schema,agent-prompt}.md
  + scripts/diff_topic.py (diffs topic JSON against catalog CSVs with a configurable star floor).
  Encodes the scan→diff→parallel-agent→merge workflow so it can be reused next time.

## Bulk review of 567 unreviewed candidates (parallel agents)
- User asked to run the 567 `unreviewed` rows in `dsh-plugin-topic-candidates.csv` through parallel
  agents, then commit + push.
- Split the 567 into **10 batches (~57 each)**; launched 10 parallel general-purpose agents,
  each reading its batch file (`/tmp/dsh_batches/batch_NN.txt`), applying `data/curation-criteria.yaml`,
  and writing verdicts to `/tmp/dsh_review_result_NN.json`. Prompt instructed: 1 WebFetch/repo,
  verdict ∈ {verified_plugin, verified_skill, watchlist, related, rejected}; write results only,
  do not touch catalog CSVs.
- All 10 returned clean JSON, exactly 567 unique repos matching the unreviewed set. Verdicts:
  **verified_plugin 425, verified_skill 13, related 123, watchlist 2, rejected 4**.
- Merge (backups /tmp/dsh_backup_20260817_164759_*):
  - `dsh-plugin-topic-candidates.csv`: 567 rows `unreviewed` → resolved; still_unreviewed=0.
  - `repositories.csv` 2298 → **2303** (+5 verified repos that were not yet in main dir:
    ccch1mneyyy/dsh-cc-tui, Ericwong5021/dsh-taskboard, GooodWei/dsh-command-context,
    openguardrails/dsh-tui, PixLunaLab/dsh-pixluna).
  - `verified-plugins.csv` 1171 → **1554** (+383 verified repos not previously in verified dir).
    NOTE: bulk-added verified rows have sparse fields (install_or_usage / last_activity empty,
    verification_source = agent notes) — flagged for later enrichment.
- Committed data CSVs + new `dsh-topic-curator` skill + this memory log; pushed to origin/main.
