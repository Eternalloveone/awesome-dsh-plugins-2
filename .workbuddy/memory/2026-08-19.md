# 2026-08-19

## dsh-plugin topic sweep — "7000+ plugins" round (full pipeline: discovery → review → merge)
- `dsh-plugin` GitHub topic has grown to **7,888 repos** (confirmed via `gh api search/repositories`).
  WebFetch pagination is now infeasible (~260 pages). Pivoted discovery to the authenticated
  `gh` Search API: `gh api "search/repositories?q=topic:dsh-plugin&sort=stars&order=desc&per_page=100"`
  → top 1000 by stars (10 pages), dedupe by lowercase name keeping max stars → `/tmp/topic_repos.json`.
- Reused the project skill `dsh-topic-curator` flow: `scripts/diff_topic.py /tmp/topic_repos.json
  --catalog-dir data --star-floor 10` → `/tmp/dsh_new_repos.json`.
  Result: **57 new repos above floor (stars>10)**, 105 skipped (≤10). Floor rule: skip stars ≤10.
- All 57 reviewed with structured verdicts (verdict == review_status):
  **45 verified_plugin (main_dir), 10 related, 2 rejected**.
  - Waves: w1.json (10, reused from earlier), waveA.json (15), waveB.json (16), waveC.json (16, regenerated).
  - Wave C verdicts were lost from /tmp on session resume → regenerated from `gh` metadata +
    README signals (verified against prior tentative verdicts; classified: futrime/webdsh=related,
    jiji262/awesome-deepseek-harness=rejected, the rest verified_plugin; PGZXB/dsh-feishu=high risk).
- Merge (backups in `/tmp/dsh_round3_20260819/`):
  - `audit-results.csv` +57 (total 1572)
  - `verified-plugins.csv` +45 (total 1602 verified)
  - `dsh-plugin-topic-candidates.csv` +57 (all 57 new repos, with review_status)
  - `repositories.csv` +45 (main-dir verified only)
  - README.md / README.en.md: `scripts/insert_verified_into_readme.py` synced verified tables.
    NOTE: it also backfilled older verified rows that were in CSV but not yet in README →
    +131 rows per file (only 45 of those are this round's new plugins).
- No duplicate rows in any CSV; README has 1076 unique repo links, zero intra-doc duplicates.
- Committed data CSVs + READMEs locally (push pending user confirmation).

## README restructure — align to website's 22 categories
- README.md / README.en.md were stale: 543 verified entries (capability rows) missing from the
  main directory; badge said `verified-1164` but CSV had 1550 distinct capability repos; snapshot
  stuck at 2026-08-17. CATALOG.md was already in sync (2296) — untouched.
- Root cause: CSV `category` taxonomy drifted to ~66 slugs; the old generator
  `scripts/insert_verified_into_readme.py` only mapped ~16, so re-running it would insert 0 of the
  missing rows (silently skipped every unmapped slug).
- Fix: added `scripts/regen_readme_sections.py` — maps each CSV `category` slug → one of the
  companion site's 22 categories (from `src/modules/plugins/merge.ts` `CATEGORIES`), regenerates the
  verified block, rewrites the nav table to the 22 anchors, and bumps the badge (→1550) + snapshot
  date (→2026-08-19). Unmapped slugs fall back to `utilities`.
- 2 new verified entries were already in the CSV working tree
  (`wenbin-wb/dsh-bridge`, `bradeGithub/DSH-Plugins-Marketplace`) — included in the regen.
  Committed locally as `626fe24` (push pending user confirmation).
- GOTCHA: the website (deepseekharnessplugins.com) reads its README source from
  `cccakeee/awesome-dsh-plugins` (hardcoded `OUR_SOURCE` in `scripts/sync-plugins.ts`), NOT this
  repo, and maps README headings → 22 cats via `SECTION_MAP`. So this restructure gives structural
  parity only; to make the live site ingest THIS repo, repoint `OUR_SOURCE` and align `SECTION_MAP`.

## README restructure → index + docs/ (verification + memory update)
- README.md / README.en.md are now an **index** (nav + top-10 per category), well under 600 lines
  (434 / 433). Preserved sections intact: CN `## 官方内置能力…`, `## 相关项目与观察名单…`,
  `## 安装与安全`, `## 贡献与维护`; EN `## Official built-in capabilities…`,
  `## Watchlist and related projects`, `## Installation and safety`, `## Contributing and maintenance`.
- Full listing moved to `docs/categories/` — **44 pages** (22 category ids × CN/EN: `<id>.md` /
  `<id>.en.md`). Each page's top link points to the matching site category
  `https://deepseekharnessplugins.com/plugins/category/<id>` (verified in first 5 lines for all 44).
- Count consistency verified: **1550 distinct repos** across all 44 pages (= badge `verified-1550` in
  both READMEs). No intra-page dup slugs; each repo appears once per language (CN+EN share the set).
- Empty categories (legit): `theme` and `voice` show `暂无` / `No verified entries yet` (both langs).
- Canonical generator is now `scripts/generate_docs.py` (reads `data/verified-plugins.csv`, maps 66
  slugs → 22 site categories via `SLUG_MAP`). Retired `insert_verified_into_readme.py` and
  `regen_readme_sections.py` are deleted.
